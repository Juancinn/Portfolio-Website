var options = {
  strings: ["Front-end Developer", "Content Creator", "Life-Enjoyer"],
  typeSpeed: 40,
  backSpeed: 40,
  backDelay: 1500,
  startDelay: 500,
  loop: true,
  showCursor: true,
  cursorChar: "|" // This defines what the cursor should look like.
};
var typed = new Typed("#dynamic-text", options);
